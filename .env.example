# Optional: enables an LLM-generated batch dashboard summary. Without this key,
# the service returns a deterministic local summary instead.
OPENROUTER_API_KEY=
# OpenRouter's free router is current as of August 2026; set a specific model if desired.
OPENROUTER_MODEL=openrouter/free
